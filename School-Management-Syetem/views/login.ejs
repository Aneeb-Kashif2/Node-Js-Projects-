<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login Page</title>
  <style>
    /* Same CSS from Signup, you can reuse the styles */
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: url('https://images.unsplash.com/photo-1601597111195-d8be44ed2c2c') no-repeat center center fixed;
      background-size: cover;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .overlay {
      background-color: rgba(0, 0, 0, 0.6);
      padding: 50px;
      border-radius: 10px;
      width: 100%;
      max-width: 450px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.3);
      color: #fff;
      animation: slideIn 1s ease forwards;
      transform: translateY(-50px);
      opacity: 0;
    }

    @keyframes slideIn {
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    .logo {
      text-align: center;
      margin-bottom: 20px;
    }

    .logo img {
      width: 80px;
      border-radius: 50%;
      border: 2px solid white;
    }

    h1 {
      text-align: center;
      margin-bottom: 25px;
      font-size: 2.2rem;
      color: #00FFFF;
    }

    label {
      display: block;
      margin-bottom: 5px;
      font-size: 1rem;
      font-weight: 600;
    }

    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 12px;
      margin-bottom: 20px;
      border: none;
      border-radius: 6px;
      font-size: 1rem;
      transition: 0.3s;
    }

    input:focus {
      outline: none;
      box-shadow: 0 0 5px #00ffff;
    }

    button {
      width: 100%;
      padding: 12px;
      background-color: #00BFFF;
      color: white;
      font-size: 1rem;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: 0.3s;
    }

    button:hover {
      background-color: #1e90ff;
      transform: scale(1.05);
    }

    .link {
      text-align: center;
      margin-top: 15px;
    }

    .link a {
      color: #00FFFF;
      text-decoration: none;
    }

    .link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="overlay">
    <div class="logo">
      <img src="https://cdn-icons-png.flaticon.com/512/3135/3135810.png" alt="Logo" />
    </div>
    <h1>Login</h1>
    <form action="/login" method="POST">
      <label>Email:</label>
      <input type="email" name="email" required />

      <label>Password:</label>
      <input type="password" name="password" required />

      <button type="submit">Login</button>
    </form>
    <div class="link">
      Don’t have an account? <a href="/signup">Signup here</a>
    </div>
  </div>

  <script>
    // Animate overlay on load
    document.querySelector('.overlay').style.animation = 'slideIn 1s ease forwards';
  </script>
</body>
</html>
